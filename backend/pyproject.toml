[project]
name = "NexusLink"
version = "0.1.0"
description = "NexusLink — Supply Chain Data Fabric & Intelligence Engine"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai",
    "python-dotenv",
    "fastapi>=0.129.0",
    "uvicorn>=0.40.0",
]
